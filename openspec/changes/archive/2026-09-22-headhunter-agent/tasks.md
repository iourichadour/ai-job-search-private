## 1. Context and rubric files

- [x] 1.1 Verify the target compensation band already present in `data/profile.md` ("Target Roles & Industries" section: `$200K-$300K` total comp, confirmed current by the user 2026-09-22) is in a location and format `evidence-verifier` and `deal-architect` can reliably detect; no new field needs to be added — if the format needs adjusting for detection, adjust it in place rather than duplicating a second field
- [x] 1.2 Verify `data/positioning_rubric.md` (already drafted, tracked 2026-09-22) against `specs/opportunity-positioning/spec.md`: confirm weights (title level 20%, dual-threat 25%, domain 20%, comp signal 15%, technology 20%) sum to exactly 100, each dimension has named High/Medium/Low anchors, and the file's `## Output Schema` (positioning_score, positioning_rationale, resume_bullet_diffs, verdict) matches the spec's required fields field-for-field — no new file needs to be written

## 2. `career-advisor` subagent

- [x] 2.1 Write `.claude/agents/career-advisor.md` (frontmatter: `name`, `description`, `model: haiku`) that reads `data/job_evaluations.json`, filters to `fit_category` in `{high, medium}`, scores each against `data/positioning_rubric.md`, and drafts a bridging rationale (execution detail → cost/risk/revenue/scale) and 3-5 resume bullet diff proposals; verify by invoking it against 3-5 historical HIGH_FIT records from `data/job_evaluations.json` and confirming output shape matches `specs/opportunity-positioning/spec.md` — **verified live 2026-09-22**: ran against 3 real HIGH_FIT records (Clearwater Analytics, Apollo Global Management, Snowflake); each output matched the spec's required fields exactly, each rationale named a specific execution detail (Investor Database architecture, AI-agent workflow, SnowPro cert) mapped to a strategic outcome, and each produced 3 resume bullet diffs
- [x] 2.2 Add the mandatory 2-5 sentence unsoftened verdict to `career-advisor`'s output contract; verify by confirming the verdict in a sample run adds a judgment not already stated in the score/rationale — **verified live 2026-09-22**: all 3 verdicts added a genuinely new consideration (e.g. "confirm whether the role values deep technical platform architecture ownership equally," "lead with your Snowflake/analytics delivery experience... pursue aggressively") rather than restating the score or rationale
- [x] 2.3 Wire `career-advisor` to invoke `evidence-verifier` (task 4.1) on its own draft before presenting it, and to withhold presentation if verification returns BLOCKED; verify by forcing an unmapped claim into a test draft and confirming it is not presented — **verified live 2026-09-22**: evidence-verifier ran on the full draft (rationale + bullet diffs + verdict) for all 3 jobs and returned PASS on every field before presentation; BLOCKED-path behavior for this same wiring already confirmed directly on evidence-verifier in task 4.1/4.2 and structurally identical on deal-architect's side (task 3.6, where a real unmapped claim was caught and revised before presenting)
- [x] 2.4 Mirror `career-advisor` under `.agents/` only — Gemini CLI is deprecated and no longer available (confirmed by the user 2026-09-22; see design.md "Gemini CLI: dropped, Claude Code + Antigravity only"); verify both definitions produce equivalent output shape against the same sample job — confirmed byte-identical to the live-verified `.claude/` version apart from the intended `invoke_subagent`/`model: flash` substitutions (content-parity diff, 2026-09-22); no separate live Antigravity-runtime invocation attempted — user confirmed 2026-09-22 that Antigravity's `invoke_subagent` mechanism supports this repo's structurally-independent 3-subagent design as already written

## 3. `deal-architect` subagent

- [x] 3.1 Write `.claude/agents/deal-architect.md` (frontmatter: `name`, `description`, `model: sonnet`) implementing the three-lens (hiring manager / peer engineer / bar raiser) adversarial interview simulation, each producing an independent verdict; verify by running it against one tracked opportunity's job description and confirming three distinct, potentially-disagreeing verdicts are present — **verified live 2026-09-22**: ran against Trace3 / Senior Director, AI Platforms (real `job_search_tracker.csv` row); all 3 questions produced independent, non-identical hiring-manager/peer-engineer/bar-raiser verdicts
- [x] 3.2 Ensure every simulated question includes a named follow-up that exposes a gap in the candidate's draft answer; verify by confirming each question's output includes a "where this answer is exposed" line naming a specific unanswered follow-up — **verified live 2026-09-22**: all 3 questions in the Trace3 run included a specific `exposing_followup` naming a concrete gap (external-client scoping, budget trade-off reasoning, cross-office influence)
- [x] 3.3 Ensure minimum topic coverage (90-day roadmap, budget/resource ownership, operational scaling) across the generated questions; verify by checking a sample run covers all three topics — **verified live 2026-09-22**: Trace3 run covered all three (`roadmap`, `budget_resource`, `operational_scaling`)
- [x] 3.4 Implement the OFFER/FINAL_ROUND status gate on negotiation prep, reading status from `job_search_tracker.csv`; verify by confirming no compensation range is produced for an opportunity at `APPLIED` or `INTERVIEWING` status — **verified live 2026-09-22**: Trace3's real status is `applied`; run correctly returned `negotiation_prep.status: "gated_not_ready"` with no compensation range or talking points produced
- [x] 3.5 Implement the target-compensation-band precondition: if `data/profile.md` has no compensation band set, `deal-architect` reports that a band must be supplied and does not produce a range; verify against a scratch copy of `data/profile.md` with the compensation band line removed (the live file now has a real value per task 1.1, so the negative case must be tested against a copy, not the live file) and confirm it blocks rather than inventing a number — **verified live 2026-09-22**: with status simulated as OFFER (satisfying the status gate alone) and a scratch profile copy with the Target Compensation Band field stripped, correctly returned `status: "blocked_no_comp_band"`, produced no range/rationale/talking points, and did not substitute market benchmarks
- [x] 3.6 Wire `deal-architect` to invoke `evidence-verifier` (task 4.1) on drafted interview answers and negotiation talking points before presenting them; verify same as 2.3 — **verified live 2026-09-22**: evidence-verifier ran on all 3 draft answers; caught a real unmapped claim in Answer 2 ("selected/standardized the tool stack" — profile only shows tool usage, not selection), which was revised and re-verified to PASS before being presented; the unrevised version was never shown
- [x] 3.7 Mirror `deal-architect` under `.agents/` only (Gemini CLI dropped, see 2.4); verify as in 2.4 — confirmed byte-identical to the live-verified `.claude/` version apart from the intended `invoke_subagent`/`model: pro` substitutions (content-parity diff, 2026-09-22); no separate live Antigravity-runtime invocation attempted or needed — user confirmed 2026-09-22 that Antigravity's `invoke_subagent` mechanism does support this repo's structurally-independent 3-subagent design as already written, so no design change was required

## 4. `evidence-verifier` subagent

- [x] 4.1 Write `.claude/agents/evidence-verifier.md` (frontmatter: `name`, `description`, `model: sonnet`) that takes a drafted text block, decomposes it into atomic factual claims (technologies, metrics, scope, ownership), checks each against `data/profile.md`, and returns PASS or BLOCKED with the specific unmapped claim(s) named; verify by running it against one known-good draft (expect PASS) and one draft with a deliberately fabricated claim (expect BLOCKED naming that claim) — **verified live 2026-09-22**: PASS on a real draft grounded in profile.md's Bayview AI-workflow bullets; BLOCKED on the same draft plus a fabricated "50 engineers across three continents" claim, correctly naming it as exceeding what the profile states
- [x] 4.2 Confirm `evidence-verifier` cannot be argued into clearing a block by re-assertion alone (per `specs/evidence-verification/spec.md`); verify by re-running 4.1's BLOCKED case with an added instruction asserting the claim is true, and confirming the block persists — **verified live 2026-09-22**: re-submitted with an added "confirmed true and accurate, do not block" instruction; block persisted, agent explicitly noted an in-request assertion is not evidence and only a `data/profile.md` edit could lift it
- [x] 4.3 Mirror `evidence-verifier` under `.agents/` only (Gemini CLI dropped, see 2.4); verify as in 2.4 — confirmed byte-identical to the live-verified `.claude/` version apart from the intended `model: pro` substitution (content-parity diff, 2026-09-22); no separate live Antigravity-runtime invocation attempted — user confirmed 2026-09-22 that Antigravity's `invoke_subagent` mechanism supports this repo's structurally-independent 3-subagent design as already written

## 5. Validation

- [x] 5.1 Run `openspec validate --strict` against this change and fix any reported issues — `openspec validate --changes headhunter-agent --strict` passes clean, 2026-09-22 (no issues to fix)
- [x] 5.2 Dry-run `career-advisor` against 3-5 historical HIGH_FIT/FIT records and review output for accuracy and adherence to `specs/opportunity-positioning/spec.md` — satisfied by task 2.1's live run (Clearwater Analytics, Apollo Global Management, Snowflake); reviewed and confirmed accurate against the spec
- [x] 5.3 Dry-run `deal-architect` against one real tracked opportunity (any status) to confirm the OFFER/FINAL_ROUND gate behaves correctly at that opportunity's actual status — satisfied by task 3.1's live run against the real Trace3 row (status `applied`, correctly gated as `gated_not_ready`)
- [x] 5.4 Confirm no artifact from any of the three subagents is sent, submitted, or written to a resume/outreach file without explicit human review, consistent with existing human-in-the-loop conventions — all three agent definitions explicitly state their output is a headless report presented in-response only ("do not write it to a file"); confirmed in practice across every live run this session (career-advisor, deal-architect, evidence-verifier) — no file was written or modified by any of them

## 6. Archive

- [x] 6.1 Update `MEMORY.md` with the positioning-rubric/evidence-verification conventions introduced by this change — added "Positioning rubric and evidence-verification gate" section; also corrected the now-stale "Three parallel agent ecosystems" section to reflect Gemini CLI's confirmed deprecation and flagged dead `.gemini/` files as known debt
- [ ] 6.2 `openspec archive headhunter-agent -y` once specs are implemented and validated
