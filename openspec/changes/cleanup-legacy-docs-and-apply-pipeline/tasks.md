## 1. Repo-Wide Audit and README Checkpoint

- [x] 1.1 Scan `.claude/`, `.gemini/`, `.agents/`, and all top-level directories; cross-reference every skill/command/tool script against live usage via grep. Findings recorded in `design.md` - Repo-Wide Audit Findings (done 2026-09-22).
- [x] 1.2 Rewrite `README.md`'s workflow diagram as a Mermaid diagram of the real, current-intended process (Gmail alerts → evaluate → `/apply` markdown draft → reviewer critique → revise → present); fix Prerequisites (drop LaTeX), fix "Fork and clone" origin (`iourichadour/ai-job-search-private`), fix File Structure to match what's actually on disk, rewrite "How `/apply` works" for the markdown workflow
- [x] 1.3 **CHECKPOINT — user review required**: present the rewritten `README.md` and the full Audit Findings (confirmed-orphaned + needs-a-decision buckets) to the user. Do not proceed to Section 2 until the user confirms which findings to delete. **Resolved 2026-09-22**: user confirmed the confirmed-dead bucket as-is, confirmed LaTeX deletion, and resolved both "needs a decision" items — consolidate to a single Gmail entry point (`/fetch-inbox` only) and fully delete the `job-scraper` skill (see 2.7-2.9 below and `design.md` Decision 6).

## 2. Delete Confirmed-Dead Artifacts (post-checkpoint)

- [x] 2.1 Delete `job_scraper/` (top-level; `.gitkeep` + gitignored `seen_jobs.json` only) and verify via `ls job_scraper 2>&1` reporting it's gone. **Do this after 2.8** — this directory is the `job-scraper` skill's dedup state, so it's only genuinely orphaned once that skill is deleted, not independently as originally audited. **Done 2026-09-22.**
- [x] 2.2 ~~Delete `_brief/`~~ **REVERSED 2026-09-22**: `_brief/mockup.html` + `_brief/report-spec.md` are kept — see `design.md` Decision 4. Not deleted.
- [x] 2.3 Delete `tools/evaluate_jobs.py`, `tools/evaluate_past_week.py`, `tools/print_data_ai_roles.py`, `tools/refetch_jobs_browser.py`, `tools/summarize_evals.py` and verify via `ls tools/` that only referenced scripts remain. **`tools/generate_mockup.py` REMOVED from this deletion list 2026-09-22** — kept and fixed (real KPIs computed from `data/job_evaluations.json`/`job_search_tracker.csv`, verified rendering correctly in-browser across all 3 tabs with 0 console errors; see `design.md` Decision 4 and `eval-dashboard/design.md` - Interim Artifact). **Correction 2026-09-22**: this box had been checked in an earlier session without the deletion actually happening — re-verified during this session (`ls tools/` showed all 5 files still present), re-confirmed zero live references (only hits were the files' own names, `README.md`'s pending-review section, and historical `documents/plans/` notes), then actually deleted all 5. Also fixed a stale doc-table row in `tools/README_EVALUATE_JOBS_GEMINI.md` that still listed `evaluate_jobs.py` as a related tool.
- [x] 2.4 Resolve the duplicate `credentials.json`: root is confirmed live (`tools/fetch_inbox.py` reads it by relative path via `InstalledAppFlow.from_client_secrets_file('credentials.json', SCOPES)`) — delete `private/credentials.json` and verify `python tools/fetch_inbox.py` still authenticates successfully afterward. **Done 2026-09-22**: deleted, re-ran `tools/fetch_inbox.py`, log confirms `Authenticating with Gmail API...` succeeded and the Gmail query ran normally (16 alert emails found).
- [x] 2.5 Delete stale `data/` scratch/backup artifacts: `data/inbox_queue.json.bkp.json`, all `data/scratch_*.json`, `data/evaluated_jobs_summary.md`. **Done 2026-09-22**: confirmed via grep that `fetch_inbox.py` only *writes* new `data/scratch_*.json` files each run (Phase 1 debug dump), never reads old ones back — matches audit's "not referenced as an input" finding. Deleted all 8 accumulated scratch files + the `.bkp.json` + the summary md.
- [x] 2.6 Delete top-level `prompts/scan_inbox_workflow.md` (distinct from `.gemini/prompts/scan_inbox_workflow.md`, which is deleted separately in 2.7 below as part of the `/scan-inbox` consolidation, not kept as "still-live" as originally audited). **Done 2026-09-22**: deleted file and the now-empty `prompts/` directory.
- [x] 2.7 **Consolidate to a single Gmail entry point (confirmed 2026-09-22)**: delete `/scan-inbox` everywhere — `.claude/commands/scan-inbox.md`, `.gemini/commands/scan-inbox.md`, `.gemini/skills/scan-inbox/`, `.agents/skills/scan-inbox/`, and `.gemini/prompts/scan_inbox_workflow.md`. Remove the two scan-inbox permission entries from `.claude/settings.local.json` (`Bash(gemini scan-inbox *)`, `Bash(gemini --skip-trust -p /scan-inbox)`). Update the description text in `.claude/agents/job-evaluator.md` and `.agents/agents/job-evaluator.agent.md` to drop the "or scan-inbox workflow" phrase, referencing only `/fetch-inbox`. Verify via `grep -rIl "scan-inbox\|scan_inbox" . --exclude-dir=.git --exclude-dir=openspec` reporting zero hits. **Done 2026-09-22**: all 5 files/dirs deleted, both permission entries removed, both job-evaluator description lines fixed (also fixed the now-stale `data/eval_batches/batch_XX.evaluated.json` example path in both, since that was scan-inbox's mechanism, not fetch-inbox's). **Scope addition discovered during verification**: the live `openspec/specs/job-evaluation/spec.md` had a real requirement scenario naming "fetch-inbox or scan-inbox" — fixed via a formally-declared Modified Capability delta (see `proposal.md` and this change's own `specs/job-evaluation/spec.md`), plus stale-reference fixes in `MEMORY.md` and `openspec/changes/centralize-config-and-private-store/tasks.md`'s now-resolved conditional.
- [x] 2.8 **Consolidate to a single Gmail entry point (confirmed 2026-09-22)**: delete the `job-scraper` skill entirely — `.claude/skills/job-scraper/SKILL.md` and `.claude/skills/job-scraper/search-queries.md`. This resolves the Danish-era `jobindex`/`jobbank` audit finding by removing the whole file, not just that sentence — `search-queries.md`'s manual LinkedIn/Google search-query strategy is fully obsolete now that Gmail alerts are the sole intended source, and the skill's own ad-hoc High/Medium/Low fit heuristic duplicates (with a weaker method) what `job-evaluator`'s 5-dimension rubric already does. Verify via `grep -rIl "job-scraper" . --exclude-dir=.git --exclude-dir=openspec` reporting hits only in `job_scraper/seen_jobs.json` (removed next, in 2.1) and this change's own planning docs. **Done 2026-09-22**: directory deleted. Remaining grep hits are `.claude/commands/setup.md` (fixed next, in 2.9) and historical/planning docs (`MEMORY.md` updated to reflect the deletion, `RESUME.md`/this change's own docs left as historical record).
- [x] 2.9 **New (2026-09-22)**: Fix `.claude/commands/setup.md` onboarding, which actively wires up both artifacts being deleted above. **Done 2026-09-22** — actual surface area was larger than originally scoped here (discovered during implementation, same confirmed decision, not a new one): removed Step A7's `/scrape` mention and its "Job search configuration (Section 9)" gap-question line; deleted Path C's entire "Section 9: Job Search Configuration" (it existed solely to populate `job-scraper/search-queries.md`, including its own Danish-portal-names question — deleting it also independently resolves that Danish-era text); removed Step 3 items 7 (`cv/main_example.tex`) and 8 (`job-scraper/search-queries.md`) — **no markdown-equivalent substitute needed for item 7**: `/apply` will draft CVs directly from `data/profile.md`/`01-candidate-profile.md` (already populated by Step 3 items 1-6), so `/setup` has no separate "master CV template" artifact to seed in the markdown workflow; updated Step 4's confirmation summary and "Try it out" callout (now points at `/fetch-inbox` + `/apply`); updated Design Principles (dropped the LaTeX mention, the Section 9 reference, the `--section search` example, and the `/scrape` mention). Verified via `grep -ni "job-scraper\|scrape\|main_example.tex\|search-quer\|Section 9\|LaTeX" .claude/commands/setup.md` reporting zero hits.
- [x] 2.10 **New (2026-09-22, user-requested — security/open-source hygiene)**: De-hardcode the personal Gmail address literal out of `tools/fetch_inbox.py:344` only — `tools/build_job_scout.py:153` has the same issue but is explicitly deferred to `centralize-config-and-private-store` (see `design.md` - Repo-Wide Audit Findings - "Deferred entirely to a follow-up change"); leave `build_job_scout.py` and `data/master_resume.md` untouched by this change. Replace the `fetch_inbox.py` literal with a value read from a new gitignored `config.local.json` at repo root (matches the existing `*.local.json` `.gitignore` pattern — no gitignore change needed), e.g. `{"job_search_email": "..."}`, loaded via a small helper (raise a clear error naming the missing file/key if absent, don't silently default to empty). Commit a tracked `config.local.example.json` showing the expected shape. Verify via `grep -n "iouri.chadour@gmail.com" tools/fetch_inbox.py` reporting zero hits (build_job_scout.py is expected to still have it until the follow-up change). **Done 2026-09-22**: added `load_local_config()` helper (raises `FileNotFoundError`/`KeyError` with clear messages), called at the top of `main()`, `gmail_query` now interpolates `config['job_search_email']`. Created `config.local.example.json` (tracked, placeholder value) and `config.local.json` (gitignored — confirmed via `git check-ignore -v`, real value). End-to-end verified: ran `python tools/fetch_inbox.py`, log shows correct Gmail query built from config and successful Gmail auth + fetch. Grep confirms zero hits.

## 3. Delete the LaTeX Pipeline

- [x] 3.1 Delete `cv/main_example.tex` and verify it no longer exists via `ls cv/` (the `cv/*.md` markdown resumes must remain untouched). **Done 2026-09-22**: deleted; all 6 `cv/*.md` files confirmed still present.
- [x] 3.2 Delete `cover_letters/cover.cls` and `cover_letters/OpenFonts/`, then remove the `cover_letters/` directory if empty; verify via `ls cover_letters/ 2>&1` reporting "No such file or directory" (or the directory being absent from `git status`/`ls .`). **Done 2026-09-22**: directory contained only `cover.cls` and `OpenFonts/`, deleted whole directory.
- [x] 3.3 Remove the LaTeX-specific sections from `.claude/skills/job-application-assistant/05-cv-templates.md` (moderncv/banking template structure, `\cventry`/`\needspace`/`\enlargethispage` guidance) and verify only markdown-relevant guidance remains. **Done 2026-09-22**: removed the moderncv template/document-structure/color-override/itemize-spacing/compile-and-inspect sections; kept and adapted the content-agnostic guidance (profile statement templates, section tailoring, length budget, relevance-weighted cutting — referenced by name in README's "What makes this workflow different" — and recommended section order) for markdown output. Verified zero LaTeX references remain.
- [x] 3.4 Remove the LaTeX-specific sections from `.claude/skills/job-application-assistant/06-cover-letter-templates.md` (`cover.cls`, `fontspec`, Raleway/Lato font wrapping) and verify only markdown-relevant guidance remains. **Done 2026-09-22**: removed the cover.cls template/document-structure/command-reference/compile-and-inspect/LaTeX-special-characters sections; kept and adapted salutation, length budget, bullet, non-English, checklist, and submission guidance for markdown output. Verified zero LaTeX references remain.

## 4. Rewrite `/apply` and the Skill for Markdown Output

- [x] 4.1 Rewrite `.claude/commands/apply.md` Step 2 to draft `applications/YYYY-MM_<Company>/cv.md` and `applications/YYYY-MM_<Company>/cover_letter.md` instead of `.tex` files, and remove the "restore" language pointing at old `cv/main_*.tex`/`cover_letters/cover_*.tex` reference files. **Done 2026-09-22**: Step 2 now derives the output folder from company name + current year-month, writes both files as markdown, and no longer reads existing `.tex` files as structural reference.
- [x] 4.2 Rewrite `.claude/commands/apply.md` Step 3 (reviewer dispatch) to reference the new markdown file paths in the `<CV_DRAFT file=...>`/`<COVER_LETTER_DRAFT file=...>` prompt template. **Done 2026-09-22**: updated both draft tags and the Part A JSON edit schema's `file` field to the new markdown paths; dropped "LaTeX" wording in the reviewer's file-skip rationale.
- [x] 4.3 Rewrite `.claude/commands/apply.md` Step 5 to replace the mandatory LaTeX compile-and-inspect step with a markdown-appropriate quality pass (re-read both files, check length/structure is reasonable for a CV and cover letter, no leftover placeholder text) — remove all `lualatex`/`xelatex` invocation and PDF-inspection instructions. **Done 2026-09-22**: Step 5 is now "Quality Pass" — re-reads both files, checks length budget/placeholder text/markdown well-formedness/section order, with relevance-weighted cutting on overflow. Zero LaTeX/compile references remain.
- [x] 4.4 Rewrite `.claude/commands/apply.md` Step 6 "Files Created" list to show the new `applications/YYYY-MM_<Company>/` markdown paths; remove the SCRUM-17 LaTeX-contradiction note at the top of the file now that it's resolved. **Done 2026-09-22**: both done — top note removed, Files Created list shows `cv.md`/`cover_letter.md` paths, closing line no longer mentions "before compiling."
- [x] 4.5 Rewrite `.claude/skills/job-application-assistant/SKILL.md` Steps 2-3 to reference markdown CV/cover-letter creation in `applications/YYYY-MM_<Company>/` instead of `cv/main_<company>.tex` / `cover_letters/cover_<company>_<role>.tex`. **Done 2026-09-22**: also fixed the Reference Files table's two "LaTeX" descriptions for 05/06 to "Markdown". Verified zero `.tex`/LaTeX references remain.
- [x] 4.6 Verify by reading through the revised `apply.md` end-to-end that every step is internally consistent (no leftover `.tex` filename in a later step referencing an earlier step's now-markdown output). **Done 2026-09-22**: grep confirms zero `.tex`/LaTeX/`main_<company>`/`cover_<company>` references; read through Steps 0-6 — file paths are consistent from Step 2's write through Step 3's review, Step 4's edits, Step 5's quality pass, and Step 6's file list. **Pre-existing gap noted, out of scope**: Step 6 says "Run the full verification checklist from `CLAUDE.md`", but `CLAUDE.md` has no such checklist (only Workflow Rules/Important Directives/Session Handoff sections) — this predates this change and isn't a LaTeX/markdown consistency issue, so left as-is; flagged for the user separately.

## 5. Finish README.md and Rewrite SETUP.md

- [x] 5.1 Update the "Customization" section's LaTeX templates subsection (currently references moderncv/`cover.cls`) to either remove it or replace it with markdown-equivalent customization guidance. **Already resolved by task 1.2's original README rewrite** — the section already reads "Both are plain markdown" with zero moderncv/cover.cls references. **Done 2026-09-22**: separately fixed the "Repo cleanup" section's own now-stale line claiming LaTeX deletion was "next" (Section 3 completed it since then).
- [x] 5.2 Verify README renders correctly in markdown preview and contains no remaining references to `lualatex`, `xelatex`, `moderncv`, `cover.cls`, or `MadsLorentzen/ai-job-search` (outside Acknowledgements). **Done 2026-09-22**: grep confirms zero `lualatex`/`xelatex`/`moderncv` hits; the one `cover.cls` hit is the "Repo cleanup" section correctly describing it in past tense as deleted; zero `MadsLorentzen/ai-job-search` hits anywhere (Acknowledgements attributes Mads Lorentzen by name/profile link, not that literal string).
- [x] 5.3 Remove the LaTeX installation section (MiKTeX/MacTeX/texlive) from `SETUP.md` entirely. **Done 2026-09-22.**
- [x] 5.4 Remove the LaTeX compile steps from `SETUP.md`'s "Test the workflow" / "Compile your documents" sections; replace with confirming the markdown files exist under `applications/YYYY-MM_Company/`. **Done 2026-09-22**: removed the whole "Compile your documents" section; "Test the workflow" now confirms `cv.md`/`cover_letter.md` exist under `applications/YYYY-MM_<Company>/`. Also fixed SETUP.md's own "Fork and clone" section (same `MadsLorentzen/ai-job-search` bug as README's, now `iourichadour/ai-job-search-private`) and removed the now-nonexistent `cv/main_example.tex` row from the "What gets populated" table (per 2.9's setup.md changes).
- [x] 5.5 Remove the LaTeX-only troubleshooting entries from `SETUP.md` (compilation errors, missing fonts, `moderncv` package). **Done 2026-09-22**: removed all three (LaTeX compilation errors, fonts not found, LaTeX-not-installed fallback); added a "Missing config.local.json" entry instead (new failure mode introduced by 2.10's de-hardcoding).
- [x] 5.6 Verify `SETUP.md` contains no remaining references to `lualatex`, `xelatex`, `moderncv`, `cover.cls`, MiKTeX, MacTeX, or texlive. **Done 2026-09-22**: grep confirms zero hits (also checked `main_example.tex` and `MadsLorentzen`, zero hits).
- [x] 5.7 **New (2026-09-22, user-requested)**: Replace `SETUP.md`'s thin "Gmail Account with Job Alerts" subsection with two real subsections. **Done 2026-09-22**: added "Create a dedicated Gmail account for job search" and "Configure Gmail API access" (8-step, verified against `tools/fetch_inbox.py`'s actual implementation, including the `config.local.json` step from 2.10) as specified below:
  - **"Create a dedicated Gmail account for job search"**: instruct the user to create a new, separate Gmail address used only for job-search alerts (not their personal/primary Gmail) — reasons to state explicitly: isolates the OAuth grant scope (`gmail.readonly`) to job-alert mail only, keeps the OAuth consent screen's test-user list and any future scope audit scoped to one purpose, lets the user revoke API access later without touching their personal account, **and avoids hardcoding a personal address anywhere** (see 5.8). Then have them subscribe that new address to LinkedIn and Indeed job alerts (the existing instruction to set up alerts still applies, just on the new address).
  - **"Configure Gmail API access"**: step-by-step, verified against `tools/fetch_inbox.py`'s actual implementation (`SCOPES = ['https://www.googleapis.com/auth/gmail.readonly']`, `InstalledAppFlow.from_client_secrets_file('credentials.json', SCOPES)`, token cached to `data/token.json`):
    1. In [Google Cloud Console](https://console.cloud.google.com/), create a new project (e.g. "ai-job-search")
    2. Enable the **Gmail API** for that project (APIs & Services -> Library)
    3. Configure the **OAuth consent screen** (External, Testing mode is sufficient for personal use) and add the dedicated job-search Gmail address as a test user
    4. Create an **OAuth 2.0 Client ID** (Application type: Desktop app)
    5. Download the credential JSON and save it as `credentials.json` in the repo root (already gitignored — never commit it)
    6. Required scope: `https://www.googleapis.com/auth/gmail.readonly` (read-only; the tooling never sends or modifies mail)
    7. Copy `config.local.example.json` to `config.local.json` (gitignored) and set `job_search_email` to the dedicated address — this is what `tools/fetch_inbox.py`'s query filter reads (see 2.8); never hardcode it into a `.py` file
    8. Run `/fetch-inbox` (or `python tools/fetch_inbox.py`) once — this opens a browser window for the OAuth consent flow; sign in with the **dedicated job-search account**, not the personal one. On success, `data/token.json` is created (gitignored) and reused on subsequent runs without re-prompting.
  - Cross-reference: this resolves the "duplicate `credentials.json`" audit item from Section 2.4 — `tools/fetch_inbox.py` reads `credentials.json` from the repo root by relative path, so root is the live copy; `private/credentials.json` is the redundant one.
- [x] 5.8 **New (2026-09-22, user-requested — open-source safety)**: Add an "If you plan to publish or open-source your fork" callout to `README.md` (near Prerequisites or as its own short section) listing every file that carries real personal data and must be scrubbed, gitignored, or excluded before making the repo public: `CLAUDE.md`, `data/profile.md`, `.claude/skills/job-application-assistant/01-candidate-profile.md`, `cv/*.md` (per-application resumes), `applications/`, `job_search_tracker.csv`, `documents/`, and `config.local.json`/`credentials.json`/`data/token.json` (already gitignored, but call them out too so the reason is clear in one place). State plainly that this repo as committed is the maintainer's own working profile, not a scrubbed template — publishing it as-is publishes that data. **Done 2026-09-22**: added as a subsection under Prerequisites, listing all 8 items with the plain statement as specified.

## 6. Add the `job-application` Spec

- [x] 6.1 Copy `specs/job-application/spec.md` from this change into `openspec/specs/job-application/spec.md` as the new main spec (following the same establishment pattern used for `inbox-ingestion` and `job-evaluation`). **Done 2026-09-22**: created `openspec/specs/job-application/spec.md`, converting the change's `## ADDED Requirements` delta format to the established `## Purpose` + `## Requirements` format matching `job-evaluation/spec.md`'s convention.
- [x] 6.2 Cross-check each requirement/scenario in the new main spec against the rewritten `apply.md` from Section 4 and verify they match (fit gate, markdown output, `applications/YYYY-MM_Company/` location, reviewer loop, no-fabrication rule). **Done 2026-09-22**: all 5 requirements verified against apply.md's actual Steps 1-4 — fit gate (Step 1), markdown output (Step 2/5), exact folder-naming convention (Step 2's own example matches the spec's), reviewer loop (Step 3/4), no-fabrication (Step 3 reviewer prompt + Step 4 point 3). No mismatches found.

## 7. Verify No Stray References

- [x] 7.1 Run `grep -rIl "lualatex\|xelatex\|moderncv\|cover\.cls" . --exclude-dir=.git --exclude-dir=openspec/changes/archive` and verify the only remaining hits are inside this change's own `openspec/changes/cleanup-legacy-docs-and-apply-pipeline/` planning artifacts (historical record) and `openspec/changes/archive/` (untouched history), not in live docs or code. **Done 2026-09-22**: hits are this change's own planning docs (expected), `openspec/specs/job-application/spec.md` (a deliberate "SHALL NOT require lualatex/xelatex" prohibition, not a residual reference), `README.md` (past-tense "has been deleted" note), and `RESUME.md` (historical session log). No live-code or live-doc residual references.
- [x] 7.2 Run `grep -rIl "MadsLorentzen/ai-job-search\b" . --exclude-dir=.git --exclude-dir=openspec/changes/archive` and verify no remaining hits outside the Acknowledgements section of README.md and this change's own planning artifacts. **Done 2026-09-22**: remaining hits are `MEMORY.md` (accurate historical/attribution fact about the repo's origin, not a clone instruction — same spirit as README's Acknowledgements), archived SCRUM-17 docs, and this change's own planning docs. README.md itself has zero hits of this literal string (Acknowledgements attributes by name/profile link instead). No stray live clone-instruction references.
- [x] 7.3 Run `grep -rl "cv/main_example\.tex\|cover_letters/cover_" . --exclude-dir=.git --exclude-dir=openspec/changes/archive` and verify no remaining hits outside archived history. **Found and fixed 2026-09-22**: `.gitignore` had 4 dead lines still referencing the deleted `.tex` patterns (`cv/main_*.tex`, `!cv/main_example.tex`, `cover_letters/cover_*.tex`, `cover_letters/Cover_*.tex`) with no markdown-era replacement — removed them (no replacement needed: markdown outputs like `cv/*.md` are already tracked, matching this private repo's existing convention). `centralize-config-and-private-store/tasks.md`'s own hit is a forward-looking note already anticipating this exact removal ("already gone per sibling change") — no fix needed there. Remaining hits are this change's own docs, README's past-tense note, and RESUME.md's session log.
- [x] 7.4 Run `grep -rIl "jobindex\|jobbank" . --exclude-dir=.git --exclude-dir=openspec/changes/archive` and verify no remaining hits outside archived history (catches the `job-scraper/search-queries.md` Danish-era sentence and anything similar). **Done 2026-09-22**: `job-scraper/search-queries.md`'s own hit is resolved (file deleted in 2.8). **Found and fixed a related staleness**: `MEMORY.md` claimed Danish job-portal skill directories (`.agents/skills/{jobbank,jobdanmark,jobindex,jobnet}-search/`) "appear fully dead" (implying undecided) — verified they're already absent from disk and corrected the wording to reflect that. Remaining hits are this change's own docs, archived SCRUM-17 docs, and RESUME.md's historical session log.
- [x] 7.5 Re-run the Section 1 audit greps against everything deleted in Section 2 and verify each deleted path/filename has zero remaining references outside `.git/` and `openspec/changes/archive/`. **Found and fixed 2 more real dead references 2026-09-22**: `.gitignore` had `job_scraper/seen_jobs.json`/`job_scraper/*.md` rules for a deleted directory (removed); `.claude/settings.local.json` had a `PowerShell(Copy-Item private/credentials.json credentials.json)` permission entry whose source file no longer exists (removed). All other remaining hits verified as this change's own docs, README's/RESUME.md's historical notes, archived changes, `documents/plans/` historical notes, and `centralize-config-and-private-store`'s forward-aware planning docs (already correctly marked `[DEAD]` or anticipating this change's deletions).
- [x] 7.6 Run `grep -n "iouri.chadour@gmail.com" tools/fetch_inbox.py` and verify zero hits (confirms 2.8's de-hardcoding — `tools/build_job_scout.py` is expected to still contain it, deferred to `centralize-config-and-private-store`); confirm `config.local.json` is untracked via `git status` (must not appear) and `config.local.example.json` is tracked. **Done 2026-09-22**: zero grep hits; `config.local.json` does not appear in `git status` (gitignored); `config.local.example.json` shows untracked, ready to `git add` in Section 8.

## 8. Commit and Close

- [ ] 8.1 Stage all changes (deletions, rewrites, new spec file) via targeted `git add` (not `git add -A`) and review `git status` before committing
- [ ] 8.2 Create a single commit with message `chore(SCRUM-17-followup): repo-wide dead-artifact cleanup, delete LaTeX pipeline, align apply.md/docs with markdown workflow, add job-application spec` and verify the commit appears in `git log`
- [ ] 8.3 Run `git status` and verify working tree is clean
