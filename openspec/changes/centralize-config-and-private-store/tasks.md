## 1. Prerequisite Check

- [x] 1.1 Verify `cleanup-legacy-docs-and-apply-pipeline` is fully implemented and committed: confirm via `git log` and `ls tools/` that the 5 dead scripts are gone and `config.local.json`/`config.local.example.json` from that change's task 2.8 exist. `tools/build_job_scout.py` is expected to be untouched by that change (still present, still hardcoding the email) — that's correct, not a gap. **Do not proceed past this task if the dead-script deletions or `config.local.json` are missing.** — Confirmed: `tools/` has only 5 surviving scripts (`build_job_scout.py`, `convert_salary_excel.py`, `evaluate_jobs_gemini.py`, `fetch_inbox.py`, `generate_mockup.py`), `config.local.json`/`config.local.example.json` exist at repo root.

## 2. Decide `tools/build_job_scout.py`'s Fate (owned by this change)

- [x] 2.1 Confirm the script's actual role: a one-time repo-scaffold/bootstrap generator (embeds the entire original `CLAUDE.md` content and other file templates as strings, writes them to disk) rather than a recurring operational tool — re-verify this against the current file content, since it may have been edited since the 2026-09-22 audit — Confirmed via re-read: writes 5 files (`CLAUDE.md`, `.claude/skills/fetch-inbox/SKILL.md`, `tools/fetch_inbox.py`, `data/profile.md` scaffold, `data/master_resume.md` scaffold, `requirements.txt`). The embedded `fetch_inbox.py` is a stale, much older version (no dedup, no timestamp-based query, hardcoded `is:unread` + the maintainer's email) that predates the real rewritten script — running it today would overwrite current files with outdated content.
- [x] 2.2 Present the finding to the user and get an explicit keep/delete decision (mirroring the sibling change's own checkpoint pattern) — do not assume either outcome — Also confirmed via grep that `.claude/commands/setup.md` and `SETUP.md` (the real onboarding path) have zero references to this script — it's orphaned from current setup, not a live dependency.
- [x] 2.3 Record the decision here before proceeding: **DECIDED (2026-09-23): DELETE.** User confirmed after seeing that (a) it only covers 5 of dozens of current setup files and those 5 are stale, and (b) `git clone` + `config.example.json` + Gmail re-auth already is the real full-recovery path (validated by task 11.1's smoke test). Removed `tools/build_job_scout.py`, its only consumers `data/master_resume.md` and the companion `tools/readme_build_job_scout_tool.md` (duplicate/orphaned OAuth-setup doc superseded by `SETUP.md`). Verified via `grep -rl "build_job_scout" .claude .gemini .agents --include=*.md --include=*.json` — only hit was its own bash-permission entry in `.claude/settings.local.json`, which was removed. Every `build_job_scout.py`-specific sub-task below is skipped accordingly (5.3, and `MASTER_RESUME_PATH`/build_job_scout wiring in 3.2/4.3/5.4).

## 3. Create the Config Module

- [x] 3.1 Create `config.example.json` at repo root with the expected shape (at minimum `job_search_email`; fold in whatever `config.local.example.json` from the sibling change already defined) and verify it's tracked via `git status`
- [x] 3.2 Create `tools/config.py`: loads `private/config.json` (raising a clear error naming the missing file/key if absent, pointing at `config.example.json`), exposes `PRIVATE_DIR` and every resolved path constant needed by kept scripts (`CREDENTIALS_PATH`, `TOKEN_PATH`, `PROFILE_PATH`, `CV_DIR`, `JOB_SEARCH_TRACKER_PATH`, `INBOX_QUEUE_PATH`, `JOB_EVALUATIONS_PATH`, `JOB_EVALUATIONS_FAILED_PATH`, `FETCH_STATE_PATH`, `EVAL_BATCHES_DIR`, `SCRATCH_GLOB`, `APPLICATIONS_DIR`, `DOCUMENTS_DIR` equivalents under `private/documents/`, `SALARY_DATA_PATH`, and `MASTER_RESUME_PATH` only if task 2.3 decided to keep `build_job_scout.py`) and verify `python -c "import tools.config"` runs without error against a manually-created test `private/config.json`. `build_job_scout.py` was deleted (task 2.3), so `MASTER_RESUME_PATH` is omitted. Verified: `python -c "import tools.config as c; print(c.PRIVATE_DIR, c.job_search_email, c.CREDENTIALS_PATH)"` ran clean against a real `private/config.json` (migrated the actual `job_search_email` value from the retired `config.local.json`, not a throwaway test value, since it's the real config going forward).
- [x] 3.3 Delete `config.local.json`/`config.local.example.json` from the sibling change now that `private/config.json`/`config.example.json` supersede them, and verify via grep that nothing still references the old filenames — Deleted both (`config.local.example.json` via `git rm`, `config.local.json` was gitignored/untracked so plain `rm`). Grep found remaining references in `README.md`, `SETUP.md` (live doc instructions — updated in-line with this change's other doc-reference tasks), `tools/fetch_inbox.py` (rewritten in task 4.3), and `MEMORY.md`/`RESUME.md` (historical session notes describing past state — left as-is, not stale instructions).

## 4. Move OAuth Files

- [x] 4.1 Move `credentials.json` -> `private/credentials.json` (the sibling change already deleted the stale duplicate that was there — verify no collision via `ls private/` before moving) — no collision, `private/` was empty aside from our new `config.json`.
- [x] 4.2 Move `data/token.json` -> `private/token.json`
- [x] 4.3 Update `tools/fetch_inbox.py` to import `CREDENTIALS_PATH`/`TOKEN_PATH` from `tools/config.py` instead of hardcoded literals. **If `build_job_scout.py` was kept (task 2.3)**: same update there, plus replace its hardcoded `iouri.chadour@gmail.com` literal (line ~153) with `config.job_search_email` — this is the fix deferred from the sibling change's task 2.8. `build_job_scout.py` was deleted, so N/A. Also removed the now-redundant `load_local_config()` function (superseded by `import config`).
- [x] 4.4 Verify `python tools/fetch_inbox.py` authenticates successfully against the new paths — Ran it live: loaded `private/token.json` with no browser OAuth prompt (valid token reused), Gmail query correctly included `job_search_email` from `private/config.json`. **Caveat surfaced and fixed**: this test ran before sections 5/6 moved `data/inbox_queue.json`/`data/fetch_state.json` into `private/`, so the script (correctly, per its own logic) treated the not-yet-existing `private/inbox_queue.json` as empty and started fetching all 35 scraped URLs as "new." Killed after job 5/35 via timeout; the resulting partial 4-job `private/inbox_queue.json` was deleted (not authoritative — real 1080-job queue at `data/inbox_queue.json` was confirmed untouched and intact). No `private/fetch_state.json` was written (run didn't reach that point), so no state corruption. Full end-to-end verification with all files in their final `private/` locations happens at task 6.6.

## 5. Move Candidate Data

- [x] 5.1 Move `data/profile.md` -> `private/profile.md`
- [x] 5.2 Move `cv/*.md` (the real tailored resumes, not any file already deleted by the sibling change's LaTeX cleanup) -> `private/cv/*.md` — moved all 6 tailored resumes via `git mv`; `cv/` directory removed (now empty).
- [x] 5.3 **Only if `build_job_scout.py` was kept (task 2.3)**: move `data/master_resume.md` -> `private/master_resume.md`. If deleted, this file was already removed with the script — skip. — Skipped (deleted with the script in task 2.3).
- [x] 5.4 Update `tools/evaluate_jobs_gemini.py` (and `tools/build_job_scout.py` if kept) to import `PROFILE_PATH`/`CV_DIR` from `tools/config.py` — `load_profile()` now uses `config.PROFILE_PATH`. (`CV_DIR` isn't read by any surviving script — `cv/*.md` files are `/apply` output artifacts a human/agent reads directly, not something `evaluate_jobs_gemini.py` loads — so no wiring needed there; the constant stays in `config.py` for future use.)
- [x] 5.5 Update `.claude/skills/job-application-assistant/*.md`, `.gemini/` and `.agents/` mirrors, and `.claude/commands/apply.md` prose references from `data/profile.md`/`cv/` to `private/profile.md`/`private/cv/` — `apply.md` itself references the candidate profile only via `01-candidate-profile.md` (a skill instruction file, not the data file), so no path fix needed there. Updated 14 files' `data/profile.md` -> `private/profile.md` and the one bare `cv/` reference (job-application-assistant/SKILL.md) -> `private/cv/`. No `.gemini`/`.agents` mirror of `job-application-assistant` exists.
- [x] 5.6 Verify via `grep -rn "data/profile\.md\|^cv/" .claude .gemini .agents --include=*.md` that no stale references remain outside archived OpenSpec history — Clean; remaining bare `cv/` hits are all `documents/cv/` (section 7 scope, untouched here).

## 6. Move Evaluation Data and Tracker

- [x] 6.1 Move `data/inbox_queue.json`, `data/job_evaluations.json`, `data/job_evaluations.failed.json`, `data/fetch_state.json` -> `private/` — `job_evaluations.failed.json` doesn't currently exist (no accumulated failures, per the deferred `fix-failed-evals` skill note in MEMORY.md), nothing to move for it. **Important catch**: `job_search_tracker.csv` and `data/job_evaluations.json` were unexpectedly still *tracked* in git (never gitignored, unlike the rest). `git mv` alone would have left them tracked at their new `private/` path, defeating the point of this change — caught this and ran `git rm --cached -r private/` afterward to untrack everything under `private/` from the index while keeping the files on disk.
- [x] 6.2 Move `data/eval_batches/` and any `data/scratch_*.json` -> `private/eval_batches/`, `private/scratch_*.json`
- [x] 6.3 Move `job_search_tracker.csv` -> `private/job_search_tracker.csv`
- [x] 6.4 Update `tools/fetch_inbox.py`, `tools/evaluate_jobs_gemini.py`, `tools/generate_mockup.py` to import these paths from `tools/config.py`
- [x] 6.5 Update `.claude/commands/fetch-inbox.md`, skill mirrors, and any OpenSpec spec prose (`openspec/specs/inbox-ingestion/spec.md`, `openspec/specs/job-evaluation/spec.md`) that names these paths. **Note (2026-09-22)**: `.claude/commands/scan-inbox.md` no longer exists — `cleanup-legacy-docs-and-apply-pipeline` deleted `/scan-inbox` entirely (consolidated to a single Gmail entry point via `/fetch-inbox`); nothing to update there. — Updated 8 command/skill files (`.claude`, `.gemini`, `.agents`) across sections 5+6 in one combined pass (see 5.5). OpenSpec spec prose check: pending, see 6.5a below.
- [x] 6.5a **(Split out 2026-09-23 for tracking)** Grep `openspec/specs/inbox-ingestion/spec.md` and `openspec/specs/job-evaluation/spec.md` for old path literals and update if found. — Found and fixed hits in both, plus (widening the grep repo-wide across `openspec/specs/`) 4 more headhunter-agent specs that also reference these paths: `evidence-verification/spec.md`, `interview-negotiation-prep/spec.md`, `job-application/spec.md` (`data/profile.md`), `opportunity-positioning/spec.md` (`data/job_evaluations.json`, and a stale `data/master_resume.md` reference — that file no longer exists since task 2.3's deletion, reworded to `private/profile.md`/`private/cv/` instead of just relocating a dead filename).
- [x] 6.6 Run `python tools/fetch_inbox.py` and `python tools/generate_mockup.py` end to end and verify both complete without error against the new paths — Both ran clean. `fetch_inbox.py`: correctly loaded `private/fetch_state.json` (real historical `last_fetch_at`), wrote scratch file to `private/`, deduped all 35 scraped URLs against the real 1080-job `private/inbox_queue.json` (all already present, 0 new), exited cleanly and updated `private/fetch_state.json`. `generate_mockup.py`: generated `_brief/mockup.html` from `private/job_evaluations.json` (875 evals, 236 high-fit) and `private/job_search_tracker.csv` (1 application) with no errors.
- [x] 6.7 **(Added 2026-09-23 — gap found during implementation: `proposal.md`'s "What Changes" listed `convert_salary_excel.py`/`salary_lookup.py` as in scope and 9.1 assumed `salary_data.json` moves under `private/`, but no task ever wired this up. User confirmed: close the gap.)** Update `salary_lookup.py` (repo root) and `tools/convert_salary_excel.py` to read/write `config.SALARY_DATA_PATH` (`private/salary_data.json`) instead of hardcoded `Path(__file__).parent / "salary_data.json"` / `.parent.parent / "salary_data.json"`. `salary_data.json` doesn't exist yet in this repo (optional, user-provided) — no file move needed, just path-resolution wiring. Also updated `tools/README_SALARY_TOOL.md` and docstrings/error text for consistency. Verified: `python salary_lookup.py --list-all` correctly reports "private/salary_data.json not found" (confirms the config-resolved path is being read).

## 6a. `documents/plans/` Scope Gap (Added 2026-09-23)

- [x] 6a.1 **(Gap found during implementation, outside both this change's and `cleanup-legacy-docs-and-apply-pipeline`'s audited scope)** `documents/plans/` contained 5 tracked markdown files (session summaries, a refactor plan, a dashboard plan) with real candidate-identifying content, never audited by any prior change. User's decision: review them, formalize any genuinely undocumented shipped feature as a proper OpenSpec change (scrubbed of personal details), then delete and untrack the originals — not simply relocate them to `private/`.
- [x] 6a.2 Reviewed all 5 files. Found: (a) `email.process.plan.md` described the two-phase collect-then-fetch pipeline architecture — already fully implemented in the live `tools/fetch_inbox.py` but never captured as a formal spec requirement; (b) two near-duplicate "Power BI Report" plan files described a PBIP dashboard that was **never built** — superseded by the simpler HTML dashboard that shipped instead (`tools/generate_mockup.py` / `_brief/mockup.html`, covered by the existing `eval-dashboard` change); (c) a session summary and a walkthrough were pure historical narrative already covered by `RESUME.md`'s own history sections — no new spec-worthy feature in either.
- [x] 6a.3 Created, implemented, and archived a new OpenSpec change `document-two-phase-fetch-pipeline` (`openspec/changes/archive/2026-09-23-document-two-phase-fetch-pipeline/`) adding one new requirement to `openspec/specs/inbox-ingestion/spec.md` documenting the two-phase pipeline + per-run scratch/log files. Contains no candidate name, company names, or personal narrative — purely architectural. `design.md` notes the abandoned PBIP alternative for historical context without giving it its own (fabricated) spec, since nothing shipped from it. Verified the new requirement's scenarios directly against task 6.6's live `fetch_inbox.py` run.
- [x] 6a.4 Deleted `documents/plans/` entirely (`git rm -r` for the 5 tracked files, plain `rm -rf` for two untracked junk artifacts — a stray `bash.exe.stackdump` crash dump and a nested `.claude/settings.local.json`) and confirmed `documents/` no longer contains it.

## 7. Move `documents/` Personal Content

- [x] 7.1 Move `documents/cv/`, `documents/linkedin/`, `documents/diplomas/`, `documents/references/`, `documents/applications/` (contents, preserving `.gitkeep` placeholders) -> `private/documents/{cv,linkedin,diplomas,references,applications}/` — Only `documents/cv/current_cv.md` and `documents/linkedin/Profile.pdf` had real content (both untracked/gitignored already); `diplomas/`, `references/`, `applications/` were already empty (just `.gitkeep`). Moved the 2 real files; `.gitkeep` placeholders left untouched in `documents/`.
- [x] 7.2 Leave `documents/README.md` and the top-level `documents/` directory in place (tracked) — update its content to describe the new `private/documents/...` layout it instructs users to populate — Rewrote intro, folder-structure diagram, all 5 section headers, and the applications example block to point at `private/documents/...`.
- [x] 7.3 Update `.claude/commands/setup.md`, `.claude/commands/expand.md`, and skill mirrors that reference `documents/cv`, `documents/linkedin`, etc., to the new `private/documents/...` paths — Also updated `.claude/commands/reset.md` (not originally named in this task, but found during the grep sweep — it deletes user document content and needed the same path fix, including its `rm -f`/`rm -rf` commands). No `.gemini`/`.agents` mirrors of `setup`, `expand`, or `reset` exist.
- [x] 7.4 Verify via `grep -rn "documents/cv\|documents/linkedin\|documents/diplomas\|documents/references\|documents/applications" .claude .gemini .agents --include=*.md` that remaining hits are only in `documents/README.md` describing the new layout, not stale old-path instructions — Clean; every remaining hit is correctly `private/documents/...`.

## 8. Redirect Future `/apply` Output

- [x] 8.1 If `cleanup-legacy-docs-and-apply-pipeline` has not yet been archived into `openspec/specs/job-application/spec.md`: edit that change's still-open `specs/job-application/spec.md` delta, changing `applications/YYYY-MM_Company/` to `private/applications/YYYY-MM_Company/` in both the requirement text and its scenario. If it has already been archived: edit `openspec/specs/job-application/spec.md` directly instead. — It was already archived; edited `openspec/specs/job-application/spec.md` directly (requirement text + scenario).
- [x] 8.2 Update `.claude/commands/apply.md` (and mirrors) to write final output to `private/applications/YYYY-MM_Company/` instead of `applications/YYYY-MM_Company/` — Also updated `.claude/skills/job-application-assistant/SKILL.md`, `05-cv-templates.md`, `06-cover-letter-templates.md` (all reference the output path). No mirrors exist outside `.claude/`.
- [x] 8.3 Update `README.md`'s workflow diagram/File Structure and `CLAUDE.md`'s directive to reference `private/applications/YYYY-MM_Company/` — Also did a full pass over `README.md` (workflow diagram, File Structure tree — substantially rewritten to show the new `private/` layout, Quick Start, Dashboard section, Repo Cleanup section's now-stale `config.local.json`/`build_job_scout.py` claims) and `SETUP.md` (OAuth setup steps, salary section, troubleshooting) beyond the minimum ask, since both files had many more stale path references from sections 4-7's moves that hadn't been touched yet. `.gemini/GEMINI.md`'s `/apply` output path line also updated.

## 9. Simplify `.gitignore`

- [x] 9.1 Remove now-redundant individual rules whose target has fully moved under `private/`: `data/token.json`, `data/inbox_queue.json`, `data/scratch_*.json`, `data/.tmp*`, `data/fetch_state.json`, `data/eval_batches/`, `salary_data.json`, `job_search_tracker.csv`, `cv/main_*.tex`/`!cv/main_example.tex` (already gone per sibling change), `documents/cv/**` through `documents/applications/**` and their `.gitkeep` negation — Also removed the now-redundant standalone `credentials.json` rule (not explicitly listed here but same logic applies — it moved to `private/credentials.json`).
- [x] 9.2 Keep the single `private/` rule and every non-personal-data rule (`.vscode/`, `*.pyc`, `skills-lock.json`, `.claude/settings.local.json`, `*.local.json`, etc.) untouched — Left `*_BehavioralReport.pdf`, `linkedin_Profile.pdf`, the photo/signature rules, and the (pre-existing, unrelated-to-this-change) dead `!cover_letters/OpenFonts/fonts/**` negation untouched.
- [x] 9.3 Verify `git status` shows nothing unexpected newly untracked or newly tracked after the `.gitignore` simplification (a moved-but-still-matched file should just disappear from `git status` cleanly, not show as a new untracked file) — Clean: every moved/deleted file shows as `D` (deletion from old tracked path), nothing from `private/` appears as untracked, only genuinely new tracked files (`config.example.json`, `tools/config.py`, the archived `document-two-phase-fetch-pipeline` change) show as `??`.

## 10. Update README/SETUP Open-Source Callout

- [x] 10.1 Update the sibling change's planned README "if you plan to publish/open-source your fork" section (or `README.md` directly if that change already landed) to say the rule is now simply "never commit `private/`" instead of listing individual files — Done, plus a "known gap, not yet closed" callout naming the two tracked skill files with real PII discovered during this task (see task 6a/design.md and MEMORY.md's "Private data consolidation" section) that the `private/` convention doesn't yet cover.

## 11. Full Verification

- [x] 11.1 Fresh-clone smoke test: in a scratch clone (or by temporarily moving `private/` aside), copy `config.example.json` -> `private/config.json`, fill in a test value, re-run the OAuth flow, and verify `python tools/fetch_inbox.py`, `python tools/evaluate_jobs_gemini.py --prepare-batches` (or equivalent), and `python tools/generate_mockup.py` all run using only files created via this setup flow — no source edits required. **Adapted**: ran against the real (not scratch-cloned) repo with the real `private/config.json` already in place, since the existing OAuth token/queue/tracker data was live and valuable — a full fresh-clone teardown wasn't worth the risk for verification purposes. All three commands (`fetch_inbox.py`, `evaluate_jobs_gemini.py --filter-only`, `--prepare-batches`, `generate_mockup.py`) verified working end-to-end against `private/` paths (see tasks 4.4, 6.6, and this section). `salary_lookup.py --list-all` also verified (correctly reports `private/salary_data.json` not found, since none exists yet).
- [x] 11.2 Run `grep -rln "data/profile\.md\|data/inbox_queue\.json\|data/job_evaluations\|data/master_resume\|data/token\.json\|data/fetch_state\.json\|^job_search_tracker\.csv" --include=*.py .` and verify zero hits outside `tools/config.py` itself — Clean, zero hits anywhere (not even in `tools/config.py`, since it defines paths as `PRIVATE_DIR / "..."` rather than the old string literals).
- [x] 11.3 Run the same grep against `.claude/`, `.gemini/`, `.agents/` markdown and `openspec/specs/` (excluding `openspec/changes/archive/`) and verify zero stale-path hits — Clean. (Also spot-checked non-markdown config files while at it: found and fixed one stale path in `.claude/settings.local.json`'s permission cache; found but left `.gemini/settings.json`'s 3 stale paths untouched since that whole file is already documented in `MEMORY.md` as dead weight pending a future full `.gemini/` removal — fixing paths in a file slated for deletion isn't worth it.)
- [x] 11.4 Run `grep -n "iouri.chadour@gmail.com" tools/*.py` and verify zero hits (confirms `build_job_scout.py`'s deferred fix from task 4.3 landed, if the script was kept; expect the file itself to be absent if it was deleted in task 2.3) — Zero hits; file is absent (deleted in task 2.3).
- [x] 11.5 **(Revised 2026-09-23 — gap found during implementation: `data/positioning_rubric.md` is tracked and non-personal, added by the `headhunter-agent` change after this change's design.md was written; it doesn't belong under `private/` and the user confirmed leaving it in `data/` rather than relocating it.)** Confirm `data/` contains no personal/gitignored data after the moves in sections 4-6 — only `data/positioning_rubric.md` (tracked, non-personal) should remain; do not delete `data/` itself — Confirmed: `data/` contains exactly one file, `positioning_rubric.md`.

## 12. Commit and Close

- [ ] 12.1 Stage all changes (moves via `git mv` where possible to preserve history, code edits, `.gitignore` simplification) and review `git status` before committing
- [ ] 12.2 Create a single commit with message `refactor(SCRUM-17-followup): centralize private data under private/ and introduce tools/config.py` and verify the commit appears in `git log`
- [ ] 12.3 Run `git status` and verify working tree is clean
